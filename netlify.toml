[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/analyze-entry"
  to = "/.netlify/functions/analyze-entry"
  status = 200
  force = true
